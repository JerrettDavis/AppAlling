# AppAlling

This is a simple pluggable test app. It can be used to create prototypes,
as a learning aid, or as a template for your own app. Ultimately, it is
intended to be a simple, easy-to-use, testable, and extensible framework that
follows the best practices for building modern applications.

